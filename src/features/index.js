const { initCommands } = require('./commands');
const { initActions } = require('./actions');
const { initViews } = require('./views');

module.exports = {
  initCommands,
  initActions,
  initViews
};
